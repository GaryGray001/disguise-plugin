# 变身插件配置文件

# 语言设置：选择 lang 文件夹中的语言文件（如 zh_cn / en_us）
# 也可以自己新建语言文件：复制任意语言文件改名（如 fr_fr.yml）后填在这里即可
language: zh_cn

# 是否关闭 locatorBar（MC 1.21.6+ 的经验条玩家位置显示）
# true  = 关闭（显示经验条）
# false = 开启（经验条位置显示其他玩家的方位）
disable-locator-bar: true
